server:
  error:
    include-stacktrace: on_param

spring:
  datasource: #conecxão entre aplicação java e mysql
    url: jdbc:mysql://localhost:3306/anime?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate: #sempre que iniciar a aplicação atualizar o banco
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG